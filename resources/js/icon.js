import { createIcons, icons } from 'lucide';
createIcons({ icons });

import { createIcons, Menu, ArrowRight, Globe,  } from 'lucide';

createIcons({
  icons: {
    Menu,
    ArrowRight,
    Globe
  }
});
